services:
  avdb-server:
    build:
      context: .
      dockerfile: Dockerfile
    image: avdb-server:latest
    container_name: avdb-server
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      # 数据持久化（DB + 图片缓存 + 爬取状态）
      - avdb_data:/app/data
      # 开发时挂载源码（生产注释掉）
      # - ./backend:/app/backend
      # - ./magnet_scraper:/app/magnet_scraper
    environment:
      - AUTH_DISABLED=false             # 生产环境必须 false
      - SECRET_KEY=${SECRET_KEY:-}      # 留空则首次启动自动生成随机密钥持久化到 data/.secret_key
      - ADMIN_USERNAME=${ADMIN_USERNAME:-admin}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-}  # 生产环境必须设置强密码；留空则首次启动自动生成随机密码(仅输出到终端 stderr，不落日志)
      - JAVDB_URL=https://javdb.com
      - DATABASE_URL=                 # 空则用默认 SQLite /app/data/javdb.db
      - DEBUG=false
      - CD2_SSL_VERIFY=${CD2_SSL_VERIFY:-false}  # 默认 false 兼容自签证书；生产/公网部署必须设为 true（校验 CD2 TLS 证书）
      # 代理（Playwright 访问 javdb 用）
      - HTTP_PROXY=
      - HTTPS_PROXY=
      # - http_proxy=http://192.168.x.x:port
      # - https_proxy=http://192.168.x.x:port
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health/ready"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s

volumes:
  avdb_data:
